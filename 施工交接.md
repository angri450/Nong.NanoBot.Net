# 2026-06-13 施工交接

## 三项目当前状态

```
Nong.Cli.Net      4.1.2  bump ready    — 126 命令, 154 测试, 16 NuGet 包, 10/10 缺口 DONE
Nong.Toolkit.Net  解耦版              — 16 skill, plugin ver 1.0.0, 不与 CLI 版本硬绑定
Nong.NanoBot.Net  应用层 10/10 done   — 102 测试, WebUI 可用但待调试
```

三项目工作区全部干净，GitHub/Gitee/GitCode 全同步。

## NanoBot 已完工

| 功能 | 状态 |
|------|------|
| run_nong 单工具模式 | DONE — Nex-N2 Pro 流式对话 + tool calling 全通 |
| 硅基流动 provider | DONE — models/secrets/config 三文件分离 |
| NongConfirmationHook | DONE — install/token 每次确认, write 每组一次 |
| PluginManager | DONE — plugin_install/list |
| WebUI 状态面板 | DONE — 外部工具 + OCR 模型状态显示 |
| WebUI 模型选择器 | DONE — Provider 切换 + 模型下拉 |
| 停止按钮 | DONE — AbortController |
| 会话删除 | DONE — DELETE API + 前端 × 按钮 |
| nongmark 渲染 | DONE — ```nongmark 代码块绿色边框 |
| 工具事件聊天内显示 | DONE — [TOOL] xxx 系统消息 |

## NanoBot WebUI 待修（新窗口继续）

| 问题 | 说明 |
|------|------|
| JS DOM null-ref 致 UI 卡死 | 删了 HTML 元素但 JS 还在引用，加 hidden 占位未彻底解决。建议重写 elements 对象，全部加 `?.` 或 `\|\| {}` 保护 |
| 文件/工具/记忆侧栏功能 | HTML 已删，JS 引用还在。要么彻底清理 JS，要么做真功能 |
| 右面板工具详情太乱 | 显示 tool name + args + result 摘要即可 |
| thinking 推理内容不显示 | 后端 emit reasoning 事件但 Nex-N2 可能不产 reasoning |
| 写作模式 | 未实现，HTML 已移除 |
| 多 Agent 会话 | 未做 |

## 启动方式

```powershell
$env:DOTNET_ROLL_FORWARD = "LatestMajor"
cd C:\Users\Administrator\Documents\Github\Nong.NanoBot.Net\Nanobot.Web
dotnet run --urls "http://127.0.0.1:8788"
```

然后浏览器打开 `http://127.0.0.1:8788`。

配置文件位置：`~/.nanobot/secrets.json`（已含临时 key，测试完删除）。
